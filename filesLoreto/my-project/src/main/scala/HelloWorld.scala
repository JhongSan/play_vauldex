import Printer.*
@main def test =
	a("Create a Pokedex application that allows users to search pokemon details. This application accepts the name or id of a pokemon and displays the details it fetched (if it exists).This application fetches data from PokeAPI then caches them on the server and users will never call PokeAPI directly from the browser.Note: Use what you have learned in Play Framework's cache feature to implement the requirements.")
	//a("the output must be HashMap(\"Subject\" -> \"average\")")